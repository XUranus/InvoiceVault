# 发票 Excel 导出 Agent 后端设计文档

## 1. 目标

基于 Rust 实现一个后端导出 Agent，用于读取数据库中的发票数据，并按照用户提供的 Excel 模板生成结果文件。

核心要求：

* 理解并复用 Excel 模板
* 自动填充发票数据
* 自动计算小计、合计等金额
* 自动填充日期、经办信息等内容
* 保持模板原有格式不被破坏
* 支持跨平台桌面端调用

## 2. 技术栈

后端仅使用 Rust 技术栈：

* `tauri`：桌面端后端入口
* `tokio`：异步任务执行
* `sqlx` / `sea-orm`：数据库访问
* `umya-spreadsheet`：读取和写入 Excel 模板
* `serde` / `serde_json`：配置与数据序列化
* `anyhow` / `thiserror`：错误处理


## 3. 后端模块划分

```text
backend
├── commands
│   └── export_excel.rs
├── services
│   ├── export_service.rs
│   ├── template_service.rs
│   └── invoice_service.rs
├── excel
│   ├── template_parser.rs
│   ├── excel_writer.rs
│   └── formula.rs
├── storage
│   └── database.rs
└── error.rs
```

## 4. 核心流程

```text
前端发起导出请求
        ↓
Tauri Command 接收参数
        ↓
查询数据库发票数据
        ↓
读取 Excel 模板
        ↓
解析模板结构
        ↓
根据模板写入数据
        ↓
写入公式、日期、汇总信息
        ↓
保存为新的 xlsx 文件
        ↓
返回导出文件路径
```

## 5. 模板处理策略

系统不直接覆盖模板，而是：

1. 打开原始模板文件
2. 识别模板中的表头、数据区域、汇总区域
3. 在指定区域写入数据
4. 复用模板已有样式
5. 生成新的 Excel 文件

模板识别优先采用规则化方式，例如：

* 根据关键文本定位表头
* 根据“小计”“合计”“日期”等文字定位汇总区
* 根据已有样式行复制数据行格式

不建议每次导出都重新推理模板，应将模板解析结果缓存为配置，后续导出直接使用。

## 6. Excel 写入策略

写入时遵循以下原则：

* 只写入目标单元格内容
* 不修改无关单元格
* 不重建整个 Sheet
* 数据行样式从模板行复制
* 金额、日期使用 Excel 原生类型
* 汇总值优先使用 Excel 公式
* 导出文件另存，不覆盖原模板

## 7. 计算策略

计算分两类：

1. Rust 后端计算
   用于生成确定的业务结果，例如总金额、税额、数量统计等。

2. Excel 公式计算
   用于保留模板的可编辑性，例如小计、合计、行内金额计算等。

推荐做法是：
**后端保证数据正确，Excel 公式保证用户打开后可追踪。**

## 8. Agent 设计边界

Agent 只负责：

* 解析模板结构
* 判断数据应该写入哪里
* 生成或更新模板绑定配置
* 发现异常并返回错误信息

Agent 不负责：

* 直接随意修改 Excel
* 直接决定业务金额
* 绕过后端校验写入文件
* 每次导出都重新猜测模板结构

实际写入 Excel 的动作必须由确定性的 Rust 代码完成。

## 9. 异常处理

需要处理的主要异常：

* 模板文件不存在
* 模板格式不符合要求
* 找不到表头或汇总区域
* 数据量超过模板可容纳范围
* Excel 文件被占用
* 数据库查询失败
* 导出路径无写入权限

所有异常统一转换为后端错误类型，并返回给前端展示。

## 10. 推荐实现原则

* 模板解析和数据写入分离
* Agent 判断和确定性写入分离
* 原模板只读，新文件输出
* 尽量使用锚点定位，避免硬编码单元格
* 导出过程可日志追踪
* 保留模板格式优先于复杂自动化

## 11. 总结

该后端的核心不是“生成 Excel”，而是：

> 基于已有模板，稳定、可控、可追踪地填充数据。

推荐架构是：

```text
Rust Agent 负责模板理解
Rust Export Engine 负责确定性写入
Rust Database Layer 负责数据查询
Tauri Command 负责桌面端调用
```
